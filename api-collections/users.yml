info:
  name: /users
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/users
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: 
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
