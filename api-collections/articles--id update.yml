info:
  name: /articles/:id update
  type: http
  seq: 13

http:
  method: PATCH
  url: http://localhost:3000/api/articles/:id
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer TOKEN
  body:
    type: json
    data: |-
      {
        "title": "iPhone 16 test update",
        "status": "published"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
