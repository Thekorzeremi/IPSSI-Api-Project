info:
  name: /articles/:id
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:3000/api/articles/:id
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
