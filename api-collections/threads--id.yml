info:
  name: /threads/:id
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:3000/api/threads/:id
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
