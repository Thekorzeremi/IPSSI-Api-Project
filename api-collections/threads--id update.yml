info:
  name: /threads/:id update
  type: http
  seq: 19

http:
  method: PATCH
  url: http://localhost:3000/api/threads/:id
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: Bearer TOKEN
  body:
    type: json
    data: |-
      {
        "title": "Bug Face ID (resolu)"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
